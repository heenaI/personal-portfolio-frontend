module.exports = {
  content: ['./src/**/*.{html,ts}'],
  theme: {
    extend: {
      colors: {
        primary: '#007BFF',        // Bright Blue (Primary)
        secondary: '#00CFFF',      // Light Blue (Secondary)
        accent: '#003366',         // Dark Blue (Accent)
        background: '#FFFFFF',     // White Background
        text: '#2D2D2D',           // Dark Gray Text
        highlight: '#F0F0F0',      // Light Gray (Highlight)
      },
    },
  },
  plugins: [],
};